<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurfaceContainer"
    android:keepScreenOn="true">
    <!-- Titre dans le cadre en haut -->
    <TextView
        android:id="@+id/tv_top_title"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginStart="0dp"
    android:layout_marginTop="50dp"
    android:layout_marginEnd="0dp"
    android:layout_marginBottom="0dp"
    android:background="@drawable/rounded_title_background"
    android:gravity="center"
    android:padding="8dp"
    android:text="POSEIDON Drone marin"
    android:textColor="@android:color/white"
    android:textSize="20sp"
    android:textStyle="bold"
    app:layout_constraintBottom_toTopOf="@+id/map_card_container"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toTopOf="parent" />


        <!-- 1. Carte en fond -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/map_card_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    android:layout_marginTop="100dp"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintBottom_toTopOf="@+id/bottom_sheet_card"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent">

    <!-- 1. Carte en fond, maintenant à l'intérieur de la CardView -->
    <org.osmdroid.views.MapView
        android:id="@+id/mv_osmmap"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</com.google.android.material.card.MaterialCardView>

    <!-- 2. "Feuille" de données en bas (Bottom Sheet) -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/bottom_sheet_card"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="100dp"
        app:cardElevation="8dp"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.App.BottomSheet"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp">

            <!-- Zone de texte principal en 2 colonnes avec deux TextView -->
            <androidx.constraintlayout.widget.Guideline
                android:id="@+id/guideline_center"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                app:layout_constraintGuide_percent="0.5" />

            <!-- Colonne de Gauche (GPS) -->
            <TextView
                android:id="@+id/tv_text_l"
                android:background="@color/black"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:padding="8dp"
                android:textIsSelectable="true"
                android:textAppearance="?attr/textAppearanceBodyMedium"
                android:textColor="?attr/colorOnSurfaceVariant"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toStartOf="@id/guideline_center"
                tools:text="Nb satellites: 12\nLatitude: 43.123456\nLongitude: 5.678901\nPrécision: 1.5 m\nAltitude: 10.00 m\nVitesse: 5.20 km/h" />

            <!-- Colonne de Droite (Capteurs) -->
            <TextView
                android:id="@+id/tv_text_r"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:padding="8dp"
                android:textIsSelectable="true"
                android:textAppearance="?attr/textAppearanceBodyMedium"
                android:textColor="?attr/colorOnSurfaceVariant"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toEndOf="@id/guideline_center"
                app:layout_constraintEnd_toEndOf="parent"
                tools:text="temperature: 21.5 °c\nturbidite: 45.0 NTU\nacidite: 7.2\nresistance: 512 Ohms" />

            <!-- Bouton de connexion à gauche -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_connect_bluetooth"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:layout_marginEnd="8dp"
                android:text="@string/connexion_poseidon"
                app:icon="@drawable/ic_bluetooth"
                app:backgroundTint="@android:color/holo_green_dark"
                android:textColor="@android:color/white"
                app:iconTint="@android:color/white"
                app:layout_constraintTop_toBottomOf="@id/tv_text_l"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toStartOf="@+id/btn_center_map"
                app:layout_constraintBottom_toBottomOf="parent"/>

            <!-- Bouton pour centrer la carte, à droite -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_center_map"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:text="Centrer"
                app:icon="@drawable/ic_center_focus"
                app:backgroundTint="@android:color/holo_green_dark"
                android:textColor="@android:color/white"
                app:iconTint="@android:color/white"
                app:layout_constraintTop_toTopOf="@id/btn_connect_bluetooth"
                app:layout_constraintBottom_toBottomOf="@id/btn_connect_bluetooth"
                app:layout_constraintStart_toEndOf="@id/btn_connect_bluetooth"
                app:layout_constraintEnd_toEndOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- 3. Statut Mode & Batterie, comme des "Chips" flottants -->
    <com.google.android.material.chip.Chip
        android:id="@+id/tv_mode_info"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginTop="100dp"
        android:text="Mode: _"
        android:visibility="gone"
        app:chipIcon="@drawable/ic_auto_mode"
        app:chipBackgroundColor="#80333333"
        android:textColor="@android:color/white"
        app:chipIconTint="@android:color/white"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:visibility="visible"/>

    <com.google.android.material.chip.Chip
        android:id="@+id/tv_battery_info"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="100dp"
        android:layout_marginEnd="20dp"
        android:text="Bat: __%"
        android:visibility="gone"
        app:chipIcon="@drawable/ic_battery"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:visibility="visible"/>

    <!-- 4. Le TextView de navigation (inchangé, reste un overlay) -->
    <TextView
        android:id="@+id/tv_nav_info"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="#80000000"
        android:gravity="center"
        android:padding="8dp"
        android:text=""
        android:textColor="@android:color/white"
        android:textSize="16sp"
        android:visibility="gone"
        app:layout_constraintBottom_toTopOf="@id/bottom_sheet_card"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />


</androidx.constraintlayout.widget.ConstraintLayout>
